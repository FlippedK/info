Клонирование проекта в папку nameProject:
git clone https://... nameProject

Добавление файлов в коммит:
git add .

Запись коммита:
git commit -m "Коментраий коммита"

Отправить коммит:
git push

Принять изменения:
git pull

Авторство коммитов:
git config --global user.email "Ваш емайл"
git config --global user.name "Ваше Имя"